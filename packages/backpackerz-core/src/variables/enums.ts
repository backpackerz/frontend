export enum ItineraryState {
	Before = "Before",
	During = "During",
	After = "After",
}
export enum StoryType {
	Transit,
	Spot,
}
export enum StoryTransits {
	Air,
	Train,
	Metro,
	Bus,
	Walk,
	Taxi,
	Ship,
	Car,
	Etc,
}
